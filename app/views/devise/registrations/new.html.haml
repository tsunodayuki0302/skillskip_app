= form_with model: @user, url: user_registration_path, class: 'registration-main', local: true do |f|
  .form-wrap
    .form-header
      %h1.form-header-text
        新規登録
    .form-group
      .form-text-wrap
        %label.form-text アイコンを選んでください
        %span.indispensable 必須
      = f.file_field :image, id:"icon-image", autofocus: true
    .form-group
      .form-text-wrap
        %label.form-text ユーザー名(半角英数字)
        %span.indispensable 必須
      = f.text_area :identification, class:"input-default", id:"indispensable", placeholder:"例) abc123", maxlength:"40"
    .form-group
      .form-text-wrap
        %label.form-text ニックネーム
        %span.indispensable 必須
      = f.text_area :nickname, class:"input-default", id:"nickname", placeholder:"例) スキル太郎", maxlength:"30"
    .form-group
      .form-text-wrap
        %label.form-text メールアドレス
        %span.indispensable 必須
      = f.email_field :email, class:"input-default", id:"email", placeholder:"PC・携帯どちらでも可", autofocus: true
    .form-group
      .form-text-wrap
        %label.form-text パスワード
        %span.indispensable 必須
      = f.password_field :password, class:"input-default", id:"password", placeholder:"6文字以上の半角英数字", maxlength:"30"
    .form-group
      .form-text-wrap
        %label.form-text パスワード(確認)
        %span.indispensable 必須
      = f.password_field :password_confirmation, class:"input-default", id:"password-confirmation", placeholder:"同じパスワードを入力して下さい", maxlength:"30"
    .form-group
      .form-text-wrap
        %label.form-text 実績
        %span.indispensable 任意
      = f.text_area :achievement, class:"input-default", id:"password-confirmation", placeholder:"例) |
        日本〇位                                                                                         |
        〇〇大会優勝                                                                                       |
        その他実績あり", maxlength:"1000"                                                                   |
    .form-group
      .form-text-wrap
        %label.form-text 自己紹介
        %span.indispensable 必須
      = f.text_area :profile, class:"input-default", id:"password-confirmation", placeholder:"例） |
        初めまして！〇〇のゲームやっています。初心者です！よろしくお願いします！", maxlength:"1000"                                  |
    .register-btn
      = f.submit "登録する" ,class:"register-red-btn"
